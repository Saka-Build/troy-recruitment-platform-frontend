services:
  frontend:
    build:
      context: .
    image: troy-ats-frontend:3.4
    container_name: troy-ats-frontend
    ports:
      - "3000:80"
    environment:
      API_UPSTREAM: "http://13.233.44.12:8080"
    restart: unless-stopped
